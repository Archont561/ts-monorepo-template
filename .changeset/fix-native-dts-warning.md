---
"@myorg/external": patch
---

Fix the Bunup d.ts build warning (TS9007): `getNativeBinding()` now declares its return type, and the native binding shape is exported as an explicit `NativeBinding` type.
