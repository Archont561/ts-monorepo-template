{
  "name": "@myorg/template",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scaffold": {
    "default": "always",
    "selfDestruct": true,
    "scriptsToRemove": [
      "docs:sync"
    ]
  },
  "bin": {
    "mdocs": "./src/docs.ts"
  },
  "scripts": {
    "build": "mbun build ./src/index.ts --outdir ./dist --target bun --packages bundle --minify",
    "test": "mbun test tests/",
    "coverage": "mbun test tests/ --coverage --coverage-reporter lcov",
    "typecheck": "mtsc --noEmit"
  },
  "devDependencies": {
    "@clack/prompts": "^1.8.0",
    "@myorg/ts": "workspace:*"
  }
}
