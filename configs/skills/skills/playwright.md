# Playwright E2E Testing

## Commands
- `bun run test:e2e` — run E2E (auto-skips if browsers missing)
- `bunx playwright install` — install browsers

## Do NOT
- Import `bun:test` in Playwright specs.