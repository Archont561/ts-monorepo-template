# Turborepo Orchestration

Turbo manages build ordering and caching.

## Commands
- `bun run build` — build all packages
- `bun run dev` — start dev servers
- `bun run typecheck` — type-check all

## Do NOT
- Run `tsc` or `bunup` directly from root.