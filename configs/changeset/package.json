{
  "name": "@myorg/changeset",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scaffold": {
    "default": "always",
    "flag": "changeset",
    "prompt": "Configure Changesets (versioning + releases)?",
    "setup": "mchangeset init"
  },
  "bin": {
    "mchangeset": "./src/cli.ts"
  },
  "scripts": {
    "build": "mbunup",
    "dev": "mbunup --watch"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2"
  }
}
