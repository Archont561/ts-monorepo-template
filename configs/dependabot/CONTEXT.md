# CONTEXT.md — @myorg/dependabot

> Snapshot of this config's current state. Rules are in [AGENTS.md](./AGENTS.md), overview in [README.md](./README.md).

## Current state

- Owns four skeletons/fragment sets: `dependabot.base.yml`, `dependabot.yml`, `dependabot-auto-merge.base.yml`, `dependabot-auto-merge.steps.yml`.
- Generated output: `.github/dependabot.yml` and `.github/workflows/dependabot-auto-merge.yml` (both regenerated by every `docs:sync`).
- Ecosystems: npm (root `bun.lock`), cargo (`packages/native/Cargo.toml` when native is enabled), github-actions (root workflows), docker (`apps/example`).
- Auto-merge uses `dependabot/fetch-metadata@v2` and merges patch/minor after CI.

## Decisions as outcomes

- **Majors are never auto-merged** — a major bump is an API decision, not maintenance.
- **One updates block per manifest directory** — Dependabot does not walk nested manifests, so each directory is declared explicitly.

## Open

- Grouping rules are per-ecosystem and were last tuned for this repo's shape; a new package type may want its own group.

## Recent changes

| Commit | What |
| :--- | :--- |
| `05e8dc6` | root docs split; dependabot fragments unchanged |
