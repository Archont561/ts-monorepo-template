# Cargo config — per guide best practices
# Workspace target dir is shared (target/ at root)

[build]
# Uncomment to use faster linker if available (mold, lld)
# rustflags = ["-C", "link-arg=-fuse-ld=mold"]

# For cross-compilation with cargo-zigbuild:
# [target.aarch64-unknown-linux-gnu]
# linker = "aarch64-linux-gnu-gcc"

[env]
# Example: set env vars for build
# RUST_BACKTRACE = "1"

# Cargo registry caching
# [source.crates-io]
# replace-with = "vendored-sources"
